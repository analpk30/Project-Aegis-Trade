services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: guardian-app
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      PORT: 3000
      PYTHON_PORT: 5000
    restart: unless-stopped
