services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: guardian-app
    ports:
      - "5000:5000"
    environment:
      PYTHON_PORT: 5000
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    restart: unless-stopped
